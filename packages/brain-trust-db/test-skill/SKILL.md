---
name: brain-trust-db-test
description: Local test harness only — points brain-trust-cli at materialized assets.
---

# Test skill

Used as cwd for `npm run db:cli` (assets copied from `dist/assets` on `brain-trust-db` build).
