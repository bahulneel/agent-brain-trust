node_modules/
dist/
# dist-tooling/ is committed (compiled scripts); do not gitignore it.
packages/brain-trust-db/test-skill/assets/
*.tsbuildinfo
.env
.env.*
coverage/
.nyc_output/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.DS_Store
Thumbs.db
.idea/
.turbo/

# Plans are not documentation. Never commit them under docs/ (Jekyll publishes that tree).
# In-progress multi-session plans belong in /planning at the repo root, not in live docs.
# Do not commit a plan once the work it describes is done.
docs/plans/
